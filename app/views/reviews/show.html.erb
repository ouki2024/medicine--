<h1>レビュー詳細ページ</h1>
<p>Find me in app/views/reviews/show.html.erb</p>

<h1>投稿詳細</h1>

    
        <div>
            <p>ユーザーネーム：<%= @review.nickname %></p>
            <p>性別：<%= @review.gender %></p>
            <p>年代：<%= @review.age %></p>
            <p>住所：<%= @review.address %></p>
            <p>病院：<%= @review.hospital %>病院</p>
            <p>診療科：<%= @review.clinical_department %>科</p>
            <p>お薬受け取り場所：<%= @review.pharmacy %></p>
            <p>訪問回数：<%= @review.use_count %>回</p>
            <p>来訪月：<%= @review.visit_month %>月</p>
            <p>感想：<%= @review.content %></p>
        </div>
        <% if @review.user_id == current_user.id %>
            <%= link_to '編集', edit_review_path(@review) %>
            |
            <%= link_to '削除', review_path(@review), method: :delete, data: {confirm: "本当に消しますか？"} %>
        <% else %>
            <%= link_to '投稿一覧', reviews_path %>
            ｜
            <%= link_to 'トップページ', root_path %>
        <% end %>
    
    
    
    